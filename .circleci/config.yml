version: 2.1

jobs: 
  build:
    docker: 
    - image: cimg/base:2023.06
    steps:
    - checkout
    - run: 
        name: Building Image
        command: docker build . -t $CONTAINER_NAME:CIRCLE_TAG
        environment:
          CONTAINER_NAME: sample-app 

